{
  "name": "sst-sandbox",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc --noEmit",
    "test": "sst load-config -- vitest watch",
    "test-run": "sst load-config -- vitest run"
  },
  "devDependencies": {
    "@serverless-stack/cli": "^1.15.13",
    "@serverless-stack/resources": "^1.15.13",
    "@tsconfig/node16": "^1.0.3",
    "aws-cdk-lib": "2.39.1",
    "prettier": "^2.7.1",
    "typescript": "^4.8.4",
    "vitest": "^0.24.3"
  },
  "workspaces": [
    "services"
  ]
}
